# Local Data

This folder is kept in the repository to document the expected structure for local-only inputs.

Expected contents:

- `assignment 2.dta`
- `assignment_2.dta`

These files are intentionally not tracked in Git. They stay on your machine so the repository can describe the project structure without redistributing raw data.
