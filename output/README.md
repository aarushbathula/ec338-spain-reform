# Output

This folder is reserved for generated figures, tables, and run logs.

Expected subfolders:

- `output/figures/`
- `output/tables/`
- `output/logs/`

Generated files should stay untracked and be rebuilt locally from the code once the replication scripts are added.
